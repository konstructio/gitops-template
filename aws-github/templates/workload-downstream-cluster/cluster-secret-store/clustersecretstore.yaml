apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: <WORKLOAD_CLUSTER_NAME>-aws-secret-manager
  annotations:
    argocd.argoproj.io/sync-wave: '10'
spec:
  provider:
    awsSecretManager:
      region: <WORKLOAD_CLUSTER_REGION>
      role: arn:aws:iam::<WORKLOAD_CLUSTER_ACCOUNT_ID>:role/eso-<WORKLOAD_CLUSTER_NAME>
      auth:
        jwt:
          serviceAccountRef:
            name: external-secrets
            namespace: external-secrets-operator
