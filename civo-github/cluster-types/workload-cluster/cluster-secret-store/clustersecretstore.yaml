apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: <WORKLOAD_CLUSTER_NAME>-vault-kv-secret
  annotations:
    argocd.argoproj.io/sync-wave: '60'
spec:
  provider:
    vault:
      server: 'https://vault.<WORKLOAD_DOMAIN_NAME>' # todo overlay 
      # Path is the mount path of the Vault KV backend endpoint
      path: 'secret'
      version: 'v2'
      auth:
        # points to a secret that contains a vault token
        # https://www.vaultproject.io/docs/auth/token
        tokenSecretRef:
          name: "<WORKLOAD_CLUSTER_NAME>-cluster-vault-bootstrap"
          key: "vault-token"
      # todo evaluate kubernetes auth method
      # auth:
      #   kubernetes:
      #     # Path where the Kubernetes authentication backend is mounted in Vault
      #     mountPath: 'kubernetes/<WORKLOAD_CLUSTER_NAME>'
      #     # A required field containing the Vault Role to assume.
      #     role: 'external-secrets'
      #     serviceAccountRef:
      #       name: 'external-secrets'
      #       namespace: 'external-secrets-operator'
      #     # secretRef:
      #     #   namespace: 'external-secrets-operator'
      #     #   name: "external-secrets"
      #     #   key: "token"

