metaphor:
  annotations: |
    linkerd.io/inject: "enabled"
  labels: |
    mirror.linkerd.io/exported: "true"
  image:
    repository: ghcr.io/feedkray-one/metaphor
  imagePullSecrets:
    - name: docker-config
  ingress:
    className: nginx
    enabled: true
    annotations:
      cert-manager.io/cluster-issuer: 'letsencrypt-prod'
      nginx.ingress.kubernetes.io/service-upstream: "true"
    hosts:
      - host: metaphor-development.feedkray.one
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: metaphor-tls
        hosts:
          - metaphor-development.feedkray.one
  metaphor:
    host: https://metaphor-development.feedkray.one/api
    console: https://kubefirst.feedkray.one

  clusterSecretStoreName: development-vault-kv-secret
  configs:
    configOne: development-cluster-override-one
    configTwo: development-cluster-override-two
