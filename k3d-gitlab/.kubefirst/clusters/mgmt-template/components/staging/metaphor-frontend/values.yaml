metaphor-frontend:
  image:
    repository: registry.gitlab.com/<GITLAB_OWNER>/metaphor-frontend
  imagePullSecrets:
    - name: docker-config
  ingress:
    enabled: true
    annotations:
      traefik.ingress.kubernetes.io/router.middlewares: staging-redirect-to-https@kubernetescrd
    className: ''
    hosts:
      - host: metaphor-frontend-staging.<DOMAIN_NAME>
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: metaphor-frontend-staging-tls
        hosts:
          - metaphor-frontend-staging.<DOMAIN_NAME>
  metaphor:
    host: https://metaphor-frontend-staging.<DOMAIN_NAME>/api
  vaultMountPoint: kubefirst
  vaultSecretPath: staging/metaphor
  isLocal: true
